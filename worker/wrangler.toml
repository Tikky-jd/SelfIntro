name = "selfintro-ai"
main = "index.js"
compatibility_date = "2024-09-23"

# 密钥不要写在这里！用下面的命令添加（或去 Dashboard → Settings → Variables）：
#   npx wrangler secret put DEEPSEEK_KEY
#   npx wrangler secret put KIMI_KEY
#   npx wrangler secret put QWEN_KEY
#   npx wrangler secret put GEMINI_KEY
#   npx wrangler secret put GROQ_KEY
#   npx wrangler secret put OPENAI_KEY
#   npx wrangler secret put XFYUN_KEY   # 讯飞星辰 MaaS（key:secret 整体作为 Bearer Token）
#
# 变量名规则：<PROVIDER 大写>_KEY，与 worker/index.js 的 PROVIDERS 键对应。
